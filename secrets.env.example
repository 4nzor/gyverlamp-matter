# Скопируй в secrets.env и заполни (secrets.env в git не попадает).
# cp secrets.env.example secrets.env
# ./scripts/apply_secrets.sh

# Wi‑Fi роутера для лампы (GyverLamp STA)
WIFI_SSID=
WIFI_PASSWORD=
